<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SampleMailer.Views.MainPage"
             Title="{Binding Title}">

    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness" iOS="0,20,0,0"/>
    </ContentPage.Padding>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Text="New Mail" Command="{Binding GoToNewMailPageCommand}"
                Margin="10,10,10,10" HorizontalOptions="FillAndExpand" VerticalOptions="Center" HeightRequest="100"/>

        <Button Grid.Row="0" Grid.Column="1" Text="Settings" Command="{Binding GoToSettingsPageCommand}"
                Margin="0,10,10,10" HorizontalOptions="FillAndExpand" VerticalOptions="Center" HeightRequest="100"/>

    </Grid>

</ContentPage>