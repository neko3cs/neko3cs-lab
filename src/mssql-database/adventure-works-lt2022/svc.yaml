apiVersion: v1
kind: Service
metadata:
  name: adventureworks-service
spec:
  selector:
    app: adventureworks
  ports:
  - protocol: TCP
    port: 1433
    targetPort: 1433
    nodePort: 31433
  type: NodePort
