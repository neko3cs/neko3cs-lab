import { HatenaResolver } from '../../service/hatena/hatena-resolver';

describe('HatenaResolver', () => {
  it('should create an instance', () => {
    expect(new HatenaResolver()).toBeTruthy();
  });
});
